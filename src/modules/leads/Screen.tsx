"use client";

// Re-exporting the CRM Page as the "Leads" module screen for now.
// In the future, this can be refactored into a standalone component.
import CRMPage from "@/app/(dashboard)/crm/page";

export default CRMPage;
