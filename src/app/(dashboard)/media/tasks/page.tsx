"use client";

// Media Tasks page reuses the Telesales Kanban tasks system
// The tasks store is user-scoped (filters by user_id), so tasks are already per-user
export { default } from "@/app/(dashboard)/telesales/tasks/page";
